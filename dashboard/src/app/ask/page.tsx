// The /ask route exists so Shell can detect pathname === "/ask" and render
// the fullscreen ask view. Shell handles all rendering — this page returns null.
export default function AskPage() {
  return null;
}
